<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.movitech.mbox.modules.wdt.dao.WdtTTaskProjectDao">
    
    <sql id="wdtTTaskProjectColumns">
        a.id AS "id",
        a.task_id AS "taskId",
        a.project_id AS "projectId",
        a.create_by AS "createBy.id",
        a.create_date AS "createDate",
        a.update_by AS "updateBy.id",
        a.update_date AS "updateDate",
        a.del_flag AS "delFlag"
    </sql>
    
    <sql id="wdtTTaskProjectJoins">
    </sql>
    
    <select id="get" resultType="WdtTTaskProject">
        SELECT 
            <include refid="wdtTTaskProjectColumns"/>
        FROM wdt_t_task_project a
        <include refid="wdtTTaskProjectJoins"/>
        WHERE a.id = #{id}
    </select>
    
    <select id="findList" resultType="WdtTTaskProject">
        SELECT 
            <include refid="wdtTTaskProjectColumns"/>
        FROM wdt_t_task_project a
        <include refid="wdtTTaskProjectJoins"/>
        <where>
            a.del_flag = #{DEL_FLAG_NORMAL}
        </where>
        <choose>
            <when test="page !=null and page.orderBy != null and page.orderBy != ''">
                ORDER BY ${page.orderBy}
            </when>
            <otherwise>
                ORDER BY a.update_date DESC
            </otherwise>
        </choose>
    </select>
    
    <select id="findAllList" resultType="WdtTTaskProject">
        SELECT 
            <include refid="wdtTTaskProjectColumns"/>
        FROM wdt_t_task_project a
        <include refid="wdtTTaskProjectJoins"/>
        <where>
            a.del_flag = #{DEL_FLAG_NORMAL}
        </where>        
        <choose>
            <when test="page !=null and page.orderBy != null and page.orderBy != ''">
                ORDER BY ${page.orderBy}
            </when>
            <otherwise>
                ORDER BY a.update_date DESC
            </otherwise>
        </choose>
    </select>
    
    <insert id="insert">
        INSERT INTO wdt_t_task_project(
            id,
            task_id,
            project_id,
            create_by,
            create_date,
            update_by,
            update_date,
            del_flag
        ) VALUES (
            #{id},
            #{taskId},
            #{projectId},
            #{createBy.id},
            #{createDate},
            #{updateBy.id},
            #{updateDate},
            #{delFlag}
        )
    </insert>
    
    <update id="update">
        UPDATE wdt_t_task_project SET     
            task_id = #{taskId},
            project_id = #{projectId},
            update_by = #{updateBy.id},
            update_date = #{updateDate}
        WHERE id = #{id}
    </update>
    
    <update id="delete">
        UPDATE wdt_t_task_project SET 
            del_flag = #{DEL_FLAG_DELETE}
        WHERE id = #{id}
    </update>
    
    
    <select id="retrieveTaskProjectByTaskId" resultType="WdtTTaskProject">
        SELECT 
            <include refid="wdtTTaskProjectColumns"/>
        FROM wdt_t_task_project a
        <include refid="wdtTTaskProjectJoins"/>
        <where>
            a.task_id = #{taskId}
        </where>
    </select>

    
</mapper>